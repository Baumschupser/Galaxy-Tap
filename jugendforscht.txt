jugend forscht

12.03.2024 werte um 347 rum
auch um 353 rum 
bei ausschlag gehen die auf etwa 365






Lichtsensor
Wenn > 5 dann soll öffter gemessen werden und anschließend, wenn es immernoch so hoch ist, dann wird die Variable "RauchmelderON" = True gesetzt.







Aktieviert






if (RauchmelderON > 5) {
	delay(2000)
	float lux1 = lightMeter.readLightLevel();
  Serial.print("Light: ");
  Serial.print(lux1);
  Serial.println(" lx");
  delay(3000);
    Serial.print("LED Aus: ");
  digitalWrite(9, LOW);
  delay(1000);
  
}